fastapi>=0.110.0
uvicorn>=0.29.0
gradio>=4.29.0
pydantic>=2.6.0
matplotlib>=3.8.0

# Training stack — install separately on GPU machine or in Colab
# pip install torch trl unsloth transformers accelerate bitsandbytes datasets
